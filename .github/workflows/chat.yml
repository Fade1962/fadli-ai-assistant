name: NARA Realtime Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */5 * * *"

concurrency:
  group: nara-realtime-bot
  cancel-in-progress: true

jobs:
  bot:
    runs-on: ubuntu-latest
    timeout-minutes: 330

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run NARA realtime Telegram bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          PERSONAL_PROFILE_JSON: ${{ secrets.PERSONAL_PROFILE_JSON }}

          ASSISTANT_NAME: NARA
          AI_PROVIDER_ORDER: gemini,groq,openrouter
          GROQ_MODEL: qwen/qwen3.6-27b
          GROQ_VISION_MODEL: qwen/qwen3.6-27b
          GROQ_WEB_MODEL: groq/compound
          GEMINI_MODEL: gemini-2.5-flash
          OPENROUTER_MODEL: openrouter/free
          TIMEZONE: Asia/Makassar
          ALLOW_SENSITIVE_FALLBACK: "false"

          # Daily Radar is handled by daily_scout.yml so it is not sent twice.
          DIGEST_TIME: ""

        run: python -u app.py
